/**
 * @deprecated This module is no longer used. The DSFR init is handled by
 * `AdsProvider` in `components/public/ads/ads-provider.tsx` which seeds
 * `window.dsfr.mode = "react"` at module import time.
 */
export const dsfrInitHtml = "";
